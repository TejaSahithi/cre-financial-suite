import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function clear() { console.log('Deleting orphaned expenses...'); const { error: e1 } = await supabase.from('expenses').delete().is('lease_id', null).eq('source', 'lease_import'); console.log('Expenses:', e1 || 'OK'); console.log('Deleting orphaned classifications...'); const { error: e2 } = await supabase.from('expense_classifications').delete().is('lease_id', null).eq('source', 'lease_import'); console.log('Classifications:', e2 || 'OK'); } clear();